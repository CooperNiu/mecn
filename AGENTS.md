# AGENTS\.md

# 全栈项目开发规范（前端 \+ 后端 \+ TDD）

## 核心原则

1. **TDD 优先**：测试驱动开发为核心开发模式，所有核心功能遵循“红\-绿\-重构”流程

2. **类型安全**：强制使用 TypeScript（前端/Node后端），Java 后端严格执行强类型校验

3. **代码一致性**：统一格式化、命名规范、架构分层，避免个性化编码

4. **可测试性**：所有核心逻辑、工具函数、接口必须编写可独立执行的测试用例

5. **极简可读**：代码优先服务于人，注释清晰、逻辑简洁，拒绝冗余与晦涩

---

## 一、前端开发规范（主流技术栈）


### 1\.2 代码规范

#### 1\.2\.1 命名规范

- 组件文件：PascalCase，如 UserList\.tsx、LoginForm\.vue

- 函数/变量：camelCase，如 getUserInfo\(\)、userListData

- 常量：UPPER\_SNAKE\_CASE，如 API\_BASE\_URL、MAX\_PAGE\_SIZE

- 样式类：kebab\-case，如 card\-container、btn\-primary

- 接口/类型：PascalCase，如 UserInfoDTO、LoginRequest


```

#### 1\.2\.3 开发规则（强制）

- 禁止直接操作 DOM，优先使用框架内置特性（如 React 的 state、Vue 的响应式）

- 禁止硬编码，所有常量（如接口地址、状态值、提示文案）抽离到配置文件

- 接口请求必须统一封装，禁止在组件内直接写 Axios 请求，统一处理 loading、错误提示

- 禁止使用 any 类型，必须定义完整的 TS 接口/类型，不确定类型用 unknown \+ 类型断言

- 组件拆分遵循“单一职责”，单个组件代码量不超过 500 行，复杂逻辑抽离到 Hooks/工具函数

- 样式禁止使用 \!important，优先通过选择器权重、Tailwind 优先级解决样式冲突

### 1\.3 前端测试规范（TDD 适配）

- 测试工具：单元测试用 Vitest / Jest，组件测试用 React Testing Library / Vue Test Utils

- 测试范围：核心工具函数、公共组件、自定义 Hooks 必须写单测；页面组件重点测试核心交互

- 测试文件：与业务文件同目录，命名格式为 \[文件名\]\.test\.tsx/\.test\.vue

- 测试要求：覆盖正常场景、异常场景、边界值（如空值、极值、异常输入）

---

## 二、后端开发规范（主流技术栈）


### 2\.2 架构分层（强制，禁止跨层调用）

```plain text
Controller 层：接收前端请求、参数校验、返回统一响应（不写业务逻辑）
Service 层：业务逻辑核心，处理业务规则、调用 DAO 层，提供可测试的业务方法
Repository/DAO 层：数据操作层，负责与数据库交互（禁止在 Service 层手写 SQL）
DTO/VO 层：数据传输对象，入参（DTO）、出参（VO）分离，避免直接返回数据库实体
Entity/Model 层：数据库实体，与数据表一一对应，禁止包含业务逻辑
Util 层：工具类，无状态、无业务依赖，提供通用工具方法
Exception 层：统一异常处理，定义业务异常、系统异常，统一返回错误信息
```

### 2\.3 代码规范

#### 2\.3\.1 接口规范（RESTful）

- 请求方法：GET（查询）、POST

- 接口路径：小写 kebab\-case，按模块划分，如 /api/v1/users、/api/v1/orders/\{id\}

- 统一响应体（强制）：
        `\{
  \&\#34;code\&\#34;: number,  // 状态码：200=成功，400=参数错误，401=未登录，403=无权限，500=服务异常
  \&\#34;msg\&\#34;: string,   // 提示信息，成功返回\&\#34;success\&\#34;，失败返回具体错误信息
  \&\#34;data\&\#34;: any      // 响应数据，无数据返回null
\}`

- 分页规范：统一使用 page（页码，默认1）、size（每页条数，默认10）作为分页参数，返回 total（总条数）、list（当前页数据）

#### 2\.3\.2 开发规则（强制）

- 禁止在 Controller 层写业务逻辑，仅负责请求接收与响应返回

- 所有入参必须做校验（如参数非空、格式正确），使用框架校验注解（如 NestJS 的 class\-validator、Spring 的 Validator）

- 统一异常处理，禁止裸奔 try/catch，所有异常统一抛出到全局异常处理器

- 日志分级：debug（开发调试）、info（正常业务）、warn（警告）、error（错误），关键操作（如登录、支付）必须打 info 日志，异常必须打 error 日志

- SQL 禁止手写拼接，必须使用 ORM 框架的查询方法，防止 SQL 注入；复杂查询用 XML 映射（Java）或 Prisma 原生查询（Node\.js）

- 数据库表名、字段名：小写 snake\_case，与实体类属性名对应（框架自动映射）

- 禁止使用魔法值，所有常量（如状态码、业务标识）抽离到常量类

### 2\.4 后端测试规范（TDD 适配）

- 测试工具：Java 用 JUnit5，集成测试用 TestContainers（模拟数据库环境）

- 测试范围：Service 层核心业务逻辑 100% 单测覆盖；Controller 层测试接口合法性、参数校验；DAO 层测试数据操作正确性

- 测试文件：与业务文件同目录，命名格式为 \[类名\]Test\.ts/\[类名\]Test\.java

- 测试要求：单测独立、无依赖（使用 Mock 模拟外部依赖，如缓存、第三方接口），可重复执行

---

## 三、TDD 测试驱动开发规范（核心，强制遵守）

### 3\.1 TDD 核心流程（红\-绿\-重构）

1. **红（Red）**：先编写测试用例，明确功能预期，运行测试 → 测试失败（此时业务代码未实现或未完善）

2. **绿（Green）**：编写最简业务代码，仅满足测试用例的预期，不做多余优化 → 运行测试 → 测试通过

3. **重构（Refactor）**：在测试通过的基础上，优化代码结构（如抽取重复逻辑、提升可读性、优化性能），不改变功能 → 重新运行测试 → 确保测试仍通过

### 3\.2 测试分层标准

|测试类型|测试范围|覆盖率要求|执行频率|
|---|---|---|---|
|单元测试|函数、方法、组件、工具类（独立功能单元）|核心逻辑 ≥ 80%，业务关键模块 ≥ 90%|开发中实时执行、提交代码前强制执行|
|集成测试|接口调用、服务间交互、数据库操作|关键接口 ≥ 70%|代码合并到开发分支前、构建前执行|
|E2E 测试|全流程链路（前端操作 → 后端接口 → 数据库）|核心业务流程 100% 覆盖|发布前、版本迭代后执行|

### 3\.3 TDD 强制规则

- 必须先写测试用例，后写业务代码，禁止“先写业务再补测试”

- 测试用例必须覆盖：正常业务场景、异常场景（如参数错误、权限不足）、边界值（如空值、极值、临界条件）

- 测试代码与业务代码同目录存放，便于维护，随业务代码同步更新

- 禁止为了提升覆盖率写无意义测试（如仅断言 true === true），测试必须验证实际业务逻辑

- 单测必须独立，无外部依赖（使用 Mock 工具模拟数据库、缓存、第三方接口），运行速度快、可重复执行

- 重构后必须重新运行所有相关测试，确保功能未被破坏

---

## 四、Git 提交规范（强制，保证代码可追溯）

### 4\.1 分支命名规范

- main：生产分支，仅用于发布版本，禁止直接提交代码

- develop：开发分支，所有功能开发、bug 修复完成后合并到该分支

- feature/xxx：功能分支，从 develop 分支创建，命名格式为 feature/功能名称（如 feature/user\-login）

- bugfix/xxx：bug 修复分支，从 develop 分支创建，命名格式为 bugfix/问题描述（如 bugfix/token\-expire）

- release/x\.x\.x：发布分支，从 develop 分支创建，用于版本发布前的测试、修复，命名格式为 release/版本号（如 release/1\.0\.0）

- hotfix/xxx：紧急修复分支，从 main 分支创建，用于生产环境紧急 bug 修复，修复后合并到 main 和 develop 分支

### 4\.2 Commit 信息格式（强制）

```plain text
<type>(<scope>): <subject>

[可选描述信息]

[可选关闭issue]

示例：
feat(user): 新增用户登录接口
test(api): 完成用户模块 TDD 测试用例
refactor(utils): 重构日期格式化工具函数
fix(auth): 修复 token 过期无法刷新的问题
docs: 更新接口文档
```

#### type 类型说明（固定）

- feat：新增功能

- fix：修复 bug

- test：编写/修改测试用例

- refactor：重构代码（不改变功能，仅优化结构）

- docs：修改文档（接口文档、说明文档等）

- style：代码格式调整（不改变逻辑，如缩进、空格）

- chore：构建流程、依赖包更新等辅助操作

#### scope 说明

指定提交影响的范围，如 user（用户模块）、order（订单模块）、api（接口层）、utils（工具类）等，无明确范围可省略。

---

## 五、质量门禁（CI/CD 强制校验，禁止跳过）

代码提交、合并、构建前必须通过以下校验，否则禁止提交/合并：

1. 代码检查：eslint \-\-fix 无报错、无警告（前端）；后端代码符合代码规范（如 Java 符合 Alibaba Java 开发手册）

2. 格式化校验：prettier \-\-check 无差异，代码格式统一

3. 测试校验：所有单测通过，无失败用例

4. 覆盖率校验：核心模块测试覆盖率不低于规定阈值（前端≥80%，后端≥80%）

5. 构建校验：前端编译无报错、后端打包无报错，可正常运行

---

## 六、OpenCode AI 强制指令（核心，注入 AI 上下文）

OpenCode 生成代码、修改代码时，必须严格遵守以下指令，禁止违反：

1. 生成代码前，必须遵循 TDD 流程：先输出对应测试用例，再编写业务代码，确保测试用例覆盖核心场景

2. 严格遵守本文件中的前端、后端代码规范、命名规范、文件结构，禁止自定义规则

3. 所有接口、函数、组件必须添加完整的 TS 类型定义（前端/Node 后端），Java 后端必须添加完整的注释和参数校验

4. 业务逻辑必须添加清晰注释，复杂逻辑（如多条件判断、算法）需补充说明逻辑思路

5. 禁止生成冗余代码、不安全代码（如 SQL 拼接、未校验的入参）、硬编码、魔法值

6. 后端接口必须遵循 RESTful 规范，统一响应格式；前端接口请求必须使用统一封装的 Axios 方法

7. 生成的测试用例必须可独立执行、无依赖，能够准确验证业务逻辑的正确性

8. 代码重构时，必须先确保原有测试用例通过，再进行优化，优化后重新运行测试

